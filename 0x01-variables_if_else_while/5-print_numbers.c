#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main -To Prints numbers from 0 to 9.
 *
 * Return: make 0 (Successful)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
